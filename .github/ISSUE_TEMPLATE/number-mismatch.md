---
name: A number does not match
about: A figure in the documents differs from what the gate produces
labels: numbers
---

**Where** (file and line):

**What the document says:**

**What the gate says** — paste the output of the command that should produce it:

```
$ cd symbolon && python3 claims.py
```

▲ Every number in these documents is supposed to be re-derivable. If one is not, that is a
defect in the ledger (`symbolon/claims.tsv`), not a matter of opinion — please say which row.

# 契約と秤 — 言語をいくつも作るための共通部分(第十一段 / 2026-09-04)

> **問い**: 「kokkos, ptyx, melon など複数言語を同時に開発進める共通部分の切り出しの議論、
> とにかく速い言語の開発がしたい」
> 二段「boost のイメージで」/ 三段「アセンブラ級にできるか」/ 四段「見かけ上 HTML が出てくる、web 開発も」/
> 五段「契約と秤。アプローチと、君という存在。使い方、投影の仕方。既存 Web / アプリ開発への接点」/
> 六段「Merge してから続けて」⇒ 反証テストを撃った / 七段「先に推しいこう」⇒ 壁を崩した /
> 八段「推しで」⇒ 寿命を入れた。事前の条件が五度目にして初めて通った /
> 九段「相乗効果が出る形で合流させてから、本気で磨きたい」⇒ 姉妹実験 exp/03 の計器に載せた /
> 十段「推しで」⇒ A(P) を測った。底までの距離は解釈の軸でゼロだった /
> 十一段「**推しで続けて**」⇒ **昇格試験を機械にした。六門すべて通過 = 昇格可。**
> 十二段以降 ⇒ **主張を門番に繋ぎ（門⑦）、昇格先の器に名が付いた —— `symbolon/`。**
>
> **段で育てる一枚**(`gradus`)。一段は全文に当たる —— 外れた節は残さず**書き換える**。
> ◆ **規範側は `DESIGN.md`**(要件と基本設計。ここの実測から逆算で起こした)。
>   ここが *なぜそうなったか*、あちらが *何を満たすべきか*。数はここが持ち、あちらは更新の条件を持つ。
> 各言語の現在地は それぞれの README / DESIGN / NOTE が正。**ここには写さない**(写しは腐る)。

---

## 0. なぜこれをやるか —— 資産は五つ

### 「同じことをやってる人はいっぱいいる」は、正しい

| やっていること | 先行 |
|---|---|
| 塔を畳む(機械を程式に特殊化) | **Futamura 射影**(1971)/ **PyPy·RPython** / **GraalVM Truffle** |
| 所有 | **Rust**、その前に Cyclone・線形型・region(Tofte–Talpin)、近くは Vale・Verona・Austral |
| 種から世界を計算する | **demoscene / .kkrieger** |
| 1 ファイル自己完結・wasm 埋め込み | single-file HTML app の系譜 |
| LLVM を避けて自分で機械語を吐く | **Zig の self-hosted backend** / **Jai** / 遡って **Forth** |

🔴 **「この言語が優れている」という主張はしない。** 事実に反する。
◆ そのうえで実際に持っている資産は五つ。**言語そのものは、その中に入っていない。**

| 資産 | 何であるか | 現在地 |
|---|---|---|
| **① 契約** | 実行の線が **データで書いてある**(14-op / 値モデルの整数域 / object tag 表 / engine seam) | ✅ ⚠️ 育成場に住んだまま |
| **② 秤** | 外した見立てを**型として残す**測り方 | ✅ 五度生えた。⚠️ 明文化されていない。⚠️ **ms は跨いで比べない**(§1) |
| **③ アプローチ** | 一本に育てず、**所有の対象を変えて分岐**させ、器官(koinon)を共有する | ✅ 五体で実演中 |
| **④ 共作者** | 契約がデータだから、**外の体が言語を触らずに道具を足せる** | ✅ 段E〜H が実証 |
| **⑤ 投影** | 一つの substrate を面を替えて出す(ASCII / canvas / WebGL / HTML / PNG / DOM) | ✅ 四度生えた |

段E(特殊化)・段F(x86-64)・段G(wasm)・段H(engine seam)は **melon / kokkos / ptyx を一行も触らずに書けた**。
読んだのは `machine.json`・`bench_host.json`・`enc_expr` の tag 表だけ。⇒ **①と④は同じ性質の表と裏。**

そして小さい言語が死ぬ理由はほぼ常に**エコシステム**。melon engine の三原則(状態は言語 / 入力は source
非依存の event / 出力は画素でなくデータ)は、実は**「読み手がエージェントなら、その穴が浅くなる」**という主張。
⇒ **2016 年に無謀だったことが、2026 年には無謀でない。** 可能性の一番硬い根拠はここだと思う。

▲ **正直なリスク**: **「言語」が正しい単位でないかもしれない** —— 実際に価値が出たのは言語より
**①契約 と ②秤** だった。成果物はコンパイル基盤かもしれない。⇒ 居心地は悪いが、そう見えている。

---

## 1. 測った

⚠️ **まず秤の欠陥を一つ**(六段目に自分で踏んだ): この容器の絶対時間は走行間で **±40% 動く**
(同じ二値が 105.5ms → 64.1ms になった)。⇒ **ms を跨いで比べない。**
🔴 **決定的な量(命令数・確保数・バイト数)で語る。** ms は同一走行の中の比だけを読む。

### 1-1. 決定的な量 —— 走行に依らない(ここが本体)

| 程式 | 床D の実行命令 | 段E の実行命令 | 減り | 床D の arena | 段E の arena |
|---|---|---|---|---|---|
| 整数ループ(sum 1..1000) | 6,103,390 | 15,011 | **407 分の 1** | 62,097 セル | **0** |
| **閉包が主役**(非 escape 閉包 ×300) | 2,350,010 | 5,711 | **411 分の 1** | 24,123 セル | **0** |
| **cons が主役**(list 300 を積んで畳む) | 3,967,486 | 9,320 | **426 分の 1** | 39,473 セル | **300** |

| 成果物 | バイト |
|---|---|
| 段F x86-64(整数ループ / 閉包) | 187 B / 217 B |
| 段G wasm(整数ループ / 閉包) | **100 B** / **107 B** |
| **段G′ wasm(cons が主役 —— pair を線形メモリの bump heap へ)** | **226 B** |
| 段H engine wasm(状態 + step) | 133 B |
| 自己完結 HTML(段G / 段H) | **2,694 B** / **4,122 B** |

### 1-2. 🔴 反証テスト —— 「407x は整数ループの数字だろう」を壊しに行った

**予想は `probe_emit.py` の冒頭に、撃つ前に書いてある**(git が日付を持つ)。

- **407x は整数ループの数字ではなかった。** 閉包 **411** / cons **426** / 整数ループ **407** ——
  **値の種類にほぼ依らない**。塔(機械を解釈する厚み)は値の形と無関係なので、畳めば同じだけ消える。
  時間で見ても段E は床D の **646〜740x**(同一走行内の比、四本の走行で安定)。
- **閉包は消えた。** 呼び先が静的に一つに定まる(= escape しない)なら inline で丸ごと消え、**wasm 107 B** まで届く。
- **確保の 99.2% は *インタプリタ自身の* 確保だった。** cons 主役で 39,473 → **300 セル**。
  残った 300 は **list の実体そのもの**(消せない。それがデータ)。
- ⚠️ **わたしの予想は四度目に外れた** —— 「cons 主役は 50〜200x に落ちる(確保が生きているから)」と書いた。
  実測は整数ループより上。外れた理由: **確保の大半が塔の側にある**と見抜けていなかった。
- ▲ 途中、cons 側が一度 tag 11 で止まったが、あれは反証ではなく **わたしの特殊化器の穴**(car/cdr/pair 未実装)。
  塞いで測り直した。**「止まった」を「限界だった」と書かない。**

🔴 **本当の壁は別のところにあった** —— 段F/G が cons 程式を**断る**。値が pair だと register にも
i64 スロットにも置けない。⇒ 七段目でここを崩した(§1-4)。

### 1-6. 🔴 合流 —— 姉妹実験 exp/03 の init.zig に載せた(九段目)

姉妹実験 `exp/03-wasm-userland`。**PID 1 になる init 兼 本物の WASI ランタイム**(4,367 行 Zig、
libc 無し・生 syscall)。ABI は最小 —— `env.print` を import、`run()` を export。**Futamura 三射影すべてを
literal に達成済み**で、`[cost] instrs= hostcalls= mempeak=` を **seed 固定で byte 一致**して刷る。

◆ **わたしの wasm は、書き直さずにそのまま載った**(一発。ABI が偶然でなく一致していた)。

| ROM | instrs(決定的) |
|---|---|
| `compiled`(sum 1..1000、100 B) | **17,017** |
| `probe_clos`(閉包、107 B) | **6,317** |

🔴 **そして八段で測れなかった A/B が、数になった**(計器なしの走行 = 費用の真値):

| 回収 | instrs | vs なし |
|---|---|---|
| なし | **53,859** | — |
| セル単位に返す | **65,859** | **+12,000 = +22.3%** |
| **まとめて捨てる(region)** | **53,939** | **+80 命令 = +0.149%** |

- **+80 は 20 回 × 4 命令**(束縛で hp を控える 2 + scope 出口で戻す 2)。**ぴったり一致する。**
- 同じ ROM を二度走らせて 53,939 / 53,939。**決定的。**
- ms の見立て(+20〜25% / 区別がつかない)と icount(+22.3% / +0.149%)は
  **ms が見えた所では一致し、見えなかった所を icount が解いた** —— 二つの計器が互いを検算した。

⚠️ **ここでも計器が測定を支配していた** —— 計器つきで測ると *回収した方が命令が少なく* 出た
(hp が伸びないので「峰を更新する」枝が省かれる)。⇒ **峰を読む走行と費用を測る走行を分けた**(`--nopeak`)。
これで三度目 —— ms の揺れ / JIT の段 / 峰の計器。**秤は毎回、自分が測定を汚していないかから疑う。**

⚠️ **合流のとき門番が一本生えた**: init.zig は `if`(0x04)を持たない(実測「未対応 opcode 0x4 @body ── 育てる余地」)。
`block` + `br_if` + `i32.eqz` の形へ吐き直した(あちらが確実に持つ構造だけで書く)。
▲ **値を返す `if`(結果型つき block)はまだ吐けない** —— 現在の probe は使っていないが、境界として残る。

### 1-7. 🔴 A(P) —— 底までの距離は、解釈の軸ではゼロだった(十段目)

**予想は撃つ前に `symbolon/icount.sh` の頭へ書き、単独で commit してから測った**(秤 八条 #1)。

| ROM | byte-walk | **collapse = A(P)** | 比 |
|---|---|---|---|
| `compiled`(sum 1..1000) | 17,017 | **17,016** | 1.00006 |
| 回収なし | 53,859 | **53,818** | 1.0008 |
| セル単位に返す | 65,859 | **65,818** | 1.0006 |
| まとめて捨てる(region) | 53,939 | **53,898** | 1.0008 |

⚠️ **予想① は外れた** —— 「byte-walk : collapse は 2〜10x」と書いた。**実測 ≈ 1.0**。
🔴 **外れ方が答えだった**: **わたしの出力には畳む塔が無い。** exp/03 の A(P) は「interp 層を畳んだ後に
残る底」で、わたしの wasm は *程式* であって interpreter ではない ∴ **既に底に居る**。
畳んでも 41 命令(block/end 等の構造)しか減らない。
⇒ **これは段E(特殊化)が塔を畳み切っていたことの、外の計器による裏取り**。§1-2 の「407 分の 1」に底が付いた。

- ✅ **予想②③ は当たった** —— 所有の代金は **底でも同じ**:
  ΔA(region) = 53,898 − 53,818 = **+80**(byte-walk と 1 命令も違わない)/ ΔA(セル単位) = **+12,000**。
  ⇒ **回収は「解釈の厚み」ではなく「本当の仕事」。畳んでも消えない。** そして +80 は 20 回 × 4 命令のまま。

▲ **単位の線**: `instrs` は *op の数* で cycle ではない。⇒ 「底」の意味は
   **「解釈を減らして op を減らす余地はもう無い」**であって、「もっと速くできない」ではない。
   より良い codegen が *吐く op 自体* を減らす余地は残る(段F の 13x 負けと同じ軸)。
▲ **高さ非依存は自分で確かめていない** —— 塔(h=2/h=3)を建てず、collapse 値を直に取った。
   あちらの設計上 collapse は P.wasm 由来ゆえ高さに依らないが、**わたしはその不変性を撃っていない。**
※ `compiled.wasm` が 91→100 B になったのは `kernel` export(A(P) を測る口)を足したため。

```bash
ERIS_EXP03=…/exp/03-wasm-userland bash symbolon/icount.sh
```
※ **写さず指す**(ptyx の `scripts/rom/probe.sh` と同じ作法)。姉妹実験が無ければ正直に skip する。

### 1-3. 梯子 A→H(時間。同一走行内の比としてだけ読む)

| 段 | 何をしたか | 一つ前比(観測範囲) |
|---|---|---|
| 床A naive (Rust) | 文字列 dispatch + HashMap + Rc | — |
| 床B 畳み | op→enum / register→スロット添字 | 2.7x |
| 床C arena | Copy 値 + arena(Rc 外し) | 1.1x |
| 床D 線形 | 線形命令列 + オペランドスタック | 1.4x(床A 比 3.4〜4.3x) |
| **段E 特殊化** | 機械をこの程式に特殊化(第一 Futamura 射影) | **床D 比 646〜740x** |
| **段F native** | x86-64 を直に吐く(mmap + W^X) | 段E 比 **7〜11x** |
| **段G wasm** | wasm を直に吐く → V8 | 段F 比 **≈13x**(V8 が register 割付をする) |
| **段H engine** | 状態を線形メモリへ / host に駆動される | 接点(§3) |

※ Python 参照実装(`self_eval_prog`)は床A のさらに ~8.4x 遅い側。

```bash
python3 symbolon/floor_emit.py && python3 symbolon/probe_emit.py
cd symbolon && rustc -O floor_ladder.rs -o /tmp/fl
/tmp/fl                                # 梯子 A→F(四床 differential 込み)
/tmp/fl --probe probe_clos.json && node probe_run.mjs probe_clos.wasm 45750   # 閉包
/tmp/fl --probe probe_cons.json && node probe_run.mjs probe_cons.wasm 45150   # cons(段G′)
/tmp/fl --probe probe_life.json        && node probe_run.mjs probe_life.wasm      25500  # 回収なし
/tmp/fl --probe probe_life.json --own  && node probe_run.mjs probe_life.own.wasm  25500  # セル単位
/tmp/fl --probe probe_life.json --own2 && node probe_run.mjs probe_life.own2.wasm 25500  # まとめて捨てる
/tmp/fl --web    && node web_verify.mjs      # 段G: wasm + 自己完結 HTML
/tmp/fl --engine && node engine_verify.mjs   # 段H: engine seam(実 Chromium をクリックで駆動)
```

---

### 1-4. 🔴 壁を崩した —— 段G′: pair を線形メモリへ(七段目)

**やったこと**: tag も boxing も足さず、**値の形を静的に推した**。
`Kind = Int | List`(List = nil か *int の list*)を컴パイル中に伝播させ、形が決まらなければ **断る**。
形が分かれば表現は要らない —— **int は生の i64、list は番地(nil = 0)**。
`cons` は bump heap に 16 B 積み、`car`/`cdr` は `i64.load` offset 0/8、`is_pair` は `≠ 0`。

| 程式 | 段E(特殊化命令列) | **段G′(wasm)** | 成果物 | 床D 比 |
|---|---|---|---|---|
| cons が主役 | 0.0185 ms | **0.00068 ms** | **226 B** | ≈**17,800x** |
| 閉包が主役 | 0.0107 ms | **0.00012 ms** | **107 B** | ≈**60,000x** |

⚠️ **ここでも秤が仕事をした** —— 「速すぎる」ので線形性チェック(呼び 10x で時間も 10x か)を撃ったら
**4.8x / 6.2x** で鳴った。消えていたのではなく **JIT の段(tier)が測定を支配**していた。
⇒ 測る前に温める工程を足したら **10.9x / 17.1x**、しかも数字は*速く*なった。
🔴 **「速すぎたら疑う」を機構で持っていたから気づけた。**

▲ 正直な限界: 形は **Int と「int の list」だけ**(入れ子 list・混在は断る)。16 ページ固定・境界検査なし。
段F′(x86 側)は未着手 —— 同じ技法で**新しい情報が出ない**ため止めた。
※ 回収は八段目で入れた(§1-5)。※ wasm が 187→214 B に増えたのは、峰を測る計器を足したため(**八段時点の数。今の値は §1-1 の表が正**)。

### 1-5. 🔴 寿命を入れた —— 所有が買うものが、測定で決まった(八段目)

**問い**: 回収が無ければ heap は仕事に比例して伸びる ⇒ **走り続ける器(engine は毎フレーム / ptyx は再演)に載らない。**
所有で回収できるか。⇒ `probe_life`(list を 50 積んで畳む、を 20 回)で三通りを A/B。

**一意所有の判定**(`scan_owned`): box `t` の list を覗く文脈は二つだけ許す ——
① `car`/`cdr`/`pair?` の引数(覗くだけ)② `cons` の **cdr 位置**(新セルへ *移す*。spine が伸びるだけ)。
それ以外に現れたら —— `let ys = getbox(xs)` でも返り値でも —— **所有を主張しない**(安全側)。

| 回収のしかた | heap の峰 | 時間(4 走行) | wasm |
|---|---|---|---|
| **なし** | **1,000 セル** | 0.00194 / 0.00198 / 0.00213 / 0.00263 ms | 266 B |
| **セル単位に返す**(`--own`) | **50 セル** | 0.00227 / 0.00251 / 0.00255 / 0.00257 ms | 286 B |
| **まとめて捨てる**(`--own2`, region) | **50 セル** | 0.00210 / 0.00214 / 0.00220 / 0.00223 ms | 274 B |

- **有界になった**: 1,000 → **50 セル**(= 生きている list ちょうど)。本質的には「仕事に比例して伸びる」→「一定」。
- **セル単位で返すのは高い**: +20〜25%。消費のたびに「頂か?」を訊くので当然。
- ⚠️ ms ではここが**測れなかった** —— 「*なし* の分布に埋もれる。区別がつかない」で止まった。
  🔴 **九段で決着した(§1-6)。姉妹実験の決定的な計器に載せたら、+80 命令 = +0.149% と数が入った。**
- ◆ **形が答えた**: 所有が言えるなら **一つずつ返すより、scope ごと捨てる方が安い**。
  hp を控えて、束縛の scope を出る時に一命令で戻すだけ。#5 が Zig の回で書いた
  「私たちは arena 思想の終端(arena が一個 + 解放不要)に既に居る」の**一歩先** —— 所有が region の境を教える。
- ▲ 安全側の条件(揃わなければ **戻さない**): region の中から外の list を書いていない / 値として list を持ち出していない。

## 2. 二つの芯

**(a) 「減らす」と「低くする」は別の軸。** 減らす(塔を畳む)= 命令 **407〜426 分の 1**、
低くする(x86)= **7〜11x**、低くしすぎて判断を失うと **13x 損**。◆ これが 変態五十九 の「Futamura 一段目 1.1x」を説明する ——
あの時の特殊化先は**また AST** で、命令数が減っていなかった。
🔴 **低くすることは情報を増やさない。情報を使ってくれる層を取り除くだけ。**

**(b) 「アセンブラなのになぜ」は謎ではない。** 機械語は**形式**であって速さの性質ではない。速さを決めるのは
*判断*。段F は判断を一つもしていない(スタック機械の逐語訳)ので、吐いた命令列で **1 反復あたりメモリ操作 28 回**。
実測 16 cycles はそれでほぼ全部説明がつく。本当の仕事は **4 ALU 命令**だけ。
⇒ **「wasm がアセンブラに勝った」のではない。「コンパイラが逐語訳に勝った」。**
▲ 反証: 段F に割付を入れて 0.5 ns 付近へ落ちれば確定(未実施)。

**では、なぜアセンブラを吐くのか** —— **依存ゼロ**(187 B。LLVM を要求するコンパイラは*種になれない*)/
**理解**(意図と珪素の間に何も挟まらない)/ **埋め込み可能**。Zig の self-hosted backend も Jai も、
理由は実行速度でなく**独立性とコンパイル速度**だった。
🔴 **速さが欲しいなら wasm が既定でいい。** 手で吐く価値は「LLVM に割付で勝つ」ことではなく、
**「汎用 backend が導出できない情報を渡す」**こと。その情報が **所有**(§5)。

---

## 3. 接点 —— engine seam(段H。既存の Web / アプリ開発へ)

◆ **設計の線: 言語に HTML も DOM も足さない。** melon engine が既に形を持っている(状態は言語 /
入力は source 非依存の event / 出力は projection)= そのまま web の形。
⇒ **web 開発は projection を DOM にするだけ**で言語側は不変。実績: melon 不変で canvas 2D → 素の WebGL を差し替えた。

**実物**: kokkos 側で副作用の無い step 式を二本組み、状態を **wasm の線形メモリ**に置いて
`step(i64)->i64` と `memory` を export(**133 B**)。JS は `BigInt64Array` で読むだけで**規則を一行も持たない**。
秤(三点): **kokkos の機械が出した真値の軌跡** ⟷ node の毎歩再演 ⟷ **実 Chromium の本物の DOM クリック 18 回**。
**接点そのものはデータで置いた** → `engine_seam.json`。framework を一つも要求しない。

```
                    ┌→ x86-64 native (187 B)   ← 依存ゼロ・埋め込み(種)
特殊化命令列 ───────┼→ wasm (100–226 B)        ← 既定。速く、ブラウザが付いてくる
                    ├→ 自己完結 HTML (2.6 KB)   ← wasm を base64 で抱えるだけ
                    ├→ engine seam (133 B)     ← **既存 web / app はここに繋ぐ**
                    └→ polyglot PNG            ← #5 が実証済み
```

🔴 **「見かけ上 HTML が出てくる」は *配布形式* の話であって、言語の性質ではない。**
◆ `ev` は人のクリックでも agent の判断でも同じ形で届く ⇒ **同じ seam が web アプリにも agent 駆動にも使える。**

---

## 4. 所有は何のライセンスか —— 五度の訂正を経て、初めて条件が通った

| 段 | 主張 | 判定 |
|---|---|---|
| 一 | 所有は**速さ**のライセンス(残費用は refcount) | ❌ 外れ(Rc 外しは 1.1x) |
| 二 | 所有は**常駐**のライセンス | 半分だけ当たり |
| 三 | 有界性の道は三つ(GC / 所有 / **静的な死**)。一番安いのは三番目 | ✅ 当たり |
| 六 | 所有が要るのは **backend が値を置けるようにするため** | 当て先は合っていた |
| 七 | 仕事は「形」と「寿命」に割れる。**形は型推論で足りた** | ✅ 壁は形だけで崩れた |
| **八** | **寿命は所有にしか出せない。そして region で取れば費用は測れない** | ✅ **条件が通った(五度目にして初めて)** |

🔴 **結論**: **所有は速さを買わない。買うのは *有界性* —— 走り続けられること。**
そして **その代金は、まとめて捨てる形なら測定の揺れに埋もれる**(セル単位で返すと +20〜25% 払う)。
二段目の「常駐のライセンス」が、六段・七段を回って**測れる形**で戻ってきた。

◆ **設計として言えること**: 所有の出しどころは *解放の命令* ではなく **region の境**。
コンパイラに要るのは「この値はいつ死ぬか」ではなく「**この scope で生まれた物は、この scope で全部死ぬか**」。
前者は難しく、後者は `scan_owned` 程度の走査で言える —— **安い問いに置き換えられる**のが所有の値打ち。

▲ 反証条件(次): ① 入れ子 region / 複数の所有 list で費用が線形に増えないか
   ② region に収まらない寿命(scope をまたいで生きる list)が出た時、何が要るか。

## 5. 切り出し —— Boost の型(育成場と昇格)

| Boost | ここでの実体 | いま |
|---|---|---|
| 育成場 | `experiments/`(third / fourth / fifth / `lab/`) | ✅ 掟「種であって言語でない。器を先に作らない」 |
| **昇格試験** | **`symbolon/promote.sh`(六門)** | ✅ **十一段で機械になった**(下記) |
| 標準 | 14-op 契約 + 値モデルの整数域 + object tag 表 + engine seam | ✅ **`lab/contract.lock` で版と指紋が付いた**(v1.0.0) |
| 昇格先 | 共通の**床**・**特殊化器**・**投影の型** | ✅ **`symbolon/`**（2026-09-04 命名）。⚠️ **まだ空** —— 試験が保留なので住んでいない |

### 🔴 昇格試験を機械にした(十一段目)

「昇格の条件は機械で書ける」と書いた ⇒ **書けるなら書いた。**`bash lab/promote.sh`。

| 門 | 何を見るか | 結果(2026-09-04) |
|---|---|---|
| ③ 契約 | 生成し直して **指紋が錠と一致**(黙って広げていない) | ✓ v1.0.0 |
| ① 実装間の一致 | 四床 + Python が `cases.json` を通る | ✓ 食い違い 0 |
| ② 数が出ている | probe 三本が期待値と一致し **命令数を出す**(「速いはず」は昇格しない) | ✓ |
| ④ 決定性 | 同じ ROM を二度で同じ icount(要 exp/03) | ✓ 17,017 |
| ⑤ 投影 | **実ブラウザ**が頁の値を出す(段G / 段H) | ✓ |
| ⑥ 隔離 | 基準以降 **言語実装に一行も触れていない** | ✓ |
| **⑦ 主張と門番** | **文書の数が、門を撃って作り直せる**(十二段で足した) | ✓ 撃てた 33 件 |

**十一段の走行(六門・exp/03 在り): 通過 6 / 落ち 0 / 保留 0 ⇒ ✅ 昇格可。**
**十二段の走行(七門・姉妹実験 無し): 通過 5 / 落ち 0 / 保留 2 ⇒ ⏸ 保留** —— 環境が無いだけだが、**それでも「可」とは言わない**(門④⑦ の飛ばしは同じ一つの欠け = `ERIS_EXP03`)。
**十五段の走行(七門・姉妹実験 在り): 通過 7 / 落ち 0 / 保留 0 ⇒ ✅ 昇格可。**

### 🔴 飛ばしが消えた —— そして飛ばしていた門の中身は、間違っていた(十五段目)

姉妹実験の実体が手元に揃い、`ERIS_EXP03` を渡して**七門すべてを撃てた**。

- 門④ **決定性** ✓ —— 同じ ROM を二度で `instrs=17017`。**文書の主張とそのまま一致**。
- 門⑦ **主張と門番** ✓ —— **38 件すべてを門から作り直した(飛ばし 0)**。
  ⇒ 十二段から保留していた「**文書は門番に裏打ちされている**」を、初めて言ってよくなった。

🔴 **ただし一度落ちた。** 台帳の三行（回収 なし / セル単位 / region）が門の出力から取り出せなかった。
原因は **わたしが撃てない門の正規表現を、撃たずに書いていた**こと —— 実際の ROM 名は
計器なしの走行を指す `.np` 付きだった。数そのものは合っていた（53,859 / 65,859 / 53,939）。

◆ **型: 撃てない門番の中身は、検証されていない。** 門番を書いた時点では「在る」だけで、
**一度も撃てていない門は、通ることも落ちることも確かめられていない**。
⇒ 十二段で「飛ばしが在るうちは裏打ちされていると言わない」と書いたのは、正しかった。
  ⚠️ ただし理由は「数が古いかもしれない」ではなく、**門番自身が壊れているかもしれない**方だった。

- ⚠️ **skip があれば「昇格可」と言わない。** 撃てていない門を通ったと読まない。
- 🔴 **門番が落ちえることを撃った** —— `contract.lock` の指紋を一文字変えたら門③ が落ち、
  判定は **昇格不可**、`exit 1`。戻したら `exit 0`。**通るだけの門番は門番ではない。**
- ⚠️ **門番自身が false negative を出した** —— 門⑤ が `一致 ✓` と `✓ 一致` の語順違いで grep を外し、
  環境は在るのに **skip と言った**。⇒ 語順に依存しない形へ直した。**門番も測定を汚す**(四度目)。
- ⚠️ **同じ回に、もう一つ踏んだ** —— この節を書き足す編集が assertion で落ちたのに、
  **commit のメッセージは足した前提で書いた**。文書より先に主張が進んだ ⇒ 次の commit で是正。
  **型: 「書いたつもり」は書いていない。** 編集の結果は、主張する前に読み返す。
- ✅ **扉に名が付いた —— `symbolon`（割符）。** 語源と選定理由は `symbolon/README.md` の header が正。
  ⚠️ **名が付いたことと、昇格したことは別。** 器は立てたが、試験が保留なので**まだ空**。

### 🔴 主張を門番に繋いだ —— そして文書が二度、段に置いていかれていた(十二段目)

受け入れ条件 A7「数字を出した主張に、対応する門番が在る」だけが **目視**だった。
八条(秤)は *測定* を守る。だが十一段で踏んだ「書いたつもり」は **報告**の失敗で、八条の外に在った。
⇒ そこを機械にした: `lab/claims.tsv`(台帳)+ `lab/claims.py`(三関門)+ `lab/claims.skip`(免除表)、`promote.sh` の **門⑦**。

| 関門 | 何を見るか | 壊すと |
|---|---|---|
| ① 突き合わせ | 台帳の言う一文が、その文書に **literal で在るか** | 「書いたつもり」が落ちる |
| ② 再導出 | その数を **門を撃って作り直し**、一致するか | 文書の数が古いと落ちる |
| ③ 被覆 | 台帳にも免除表にも無い「量」が文書に出ていないか | 裸の主張を足すと落ちる |

🔴 **初回で落ちた。しかも、わたしが疑っていなかった所で。**
**段が文書の全体に触れていなかった事例が、二つ在った** —— どちらも誰にも気づかれずに残っていた。

| いつ | 何が起きたか | 残骸 |
|---|---|---|
| 第七段 | 閉包 wasm が 114→98 B。**表は直したが本文と図は直さなかった** | §1-2 の本文と §3 の図に `114` |
| 第九段 | `kernel` export で 98→107 B。**§1-6 に注記だけ書いた** | §1-1 の表・§1-4 の表・§3 の図が `98` のまま |

同じ理由で `91`(整数)/ `187`(cons)/ `254・271・262`(寿命三本)も古かった。実測は
**100 / 226 / 266・286・274 B**。⇒ **全部 書き換えた**(追記しない。gradus の規律)。

- 🔴 **型: 段は文書の *全体* に触れる。触れたかを人が見ると、二度見落とす。**
  ⇒ 十一段の型(「書いたつもり」)と同じ家族だが、あれは *一度の嘘*、これは **静かな腐り**。
  腐りは謝る相手が居ないので、機械しか捕まえられない。
- ◆ **数の源は二つだけ**にした —— 文書の literal と、門の出力。台帳は **三度目の写しを持たない**
  (持てば、そこが次にずれる)。主張する数だけを `⟦ ⟧` で括り、括りを外した一文が文書に在ることを確かめる。
- ◆ **免除の理由は五語しか許さない**: 再掲 / 導出 / 経緯 / 構造の定数 / 言い回し。
  ⚠️ **「門番を書くのが面倒」は理由ではない。** 免除表が伸びた分だけ、主張が裸になっている。
- 🔴 **門⑦ も落ちえることを撃った** —— ①(台帳の一文を消す)②(数を 1 ずらす)③(裸の量を足す)を
  それぞれ壊して、三度とも `✗ 判定`。**② の独立した証拠は初回の走行そのもの**(文書 `91` ⟷ 門 `100`)。
- ◆ **DESIGN.md の自己申告も機械が見る** —— あちらは「**ここは数を持たない**(数は年代記が持つ)」と
  自分で書いている ⇒ 被覆の走査に入れた。数を書いた瞬間に門⑦ が落ちる(撃って確かめた)。
- ▲ **正直に、三つ緩い。** ① 免除の鍵が *値* なので、同じ量が別の場所に出ると素通りする。
  ② icount 由来の 5 件は環境が無くて飛ばした ⇒ **「文書は門番に裏打ちされている」とはまだ言わない。**
  ③ 見るのは **数だけ**。この節を書いた時、わたしは節番号を二つ間違えて(§6/§2 → §3/§1-4)、
  **門⑦ は素通りさせた**。参照先の正しさは、まだ人が読んでいる。

---

### 🔴 住んだ —— そして「英語で出す」が原則⑤ とぶつかった(十六段目)

七門が通った ⇒ 育成場から `symbolon/` へ**実体を移した**。移した後も **通過 7 / 落ち 0 / 保留 0**。

⚠️ **公開は英語ベース**にすると決めた。ところが原則⑤（出す物は本体でも同じ文で書く／フィルタで削らない）と
ぶつかる —— **翻訳は決定的でない** ⇒ 投影器に入れると差分の理由が三つに増えて原則④ が壊れる。

◆ **解き方**: 英語の文書は *訳す* のでなく **書く**（`symbolon/README.md` / `VERIFY.md` / `MISSES.md`）。
年代記と設計書は**日本語のまま出す**。訳さないので非決定性が入らず、**失う物もゼロ**。

🔴 **移す途中で、影が壊れていたのを見つけた** —— 出荷していた `probe_emit.py` / `engine_emit.py` は
`kokkos` を import する。あれは**言語実装で、門⑥ が「symbolon の物ではない」と守っている側**。
⇒ 公開側では動かないファイルを出していた。**符号化器ではなく契約データを出す**形に直した（憲章の通り）。

🔴 **README に時間の比を書いて、門番に捕まった** —— 「native 化は 11.3×」と書いたが、今の走行は 10.7。
**八条③「ms を跨いで比べない」を、自分で書いておいて自分で破っていた**。⇒ 数を消し、決定的な事実に置き換えた。

🔴 **門⑥ が影の中で空振りの ✓ を出していた** —— 守る対象が木に無いので diff が必ず空になる。
⇒ 「触っていない」ではなく **「測れない」と言う**形へ直した。**落ちえない門は門ではない**。

- ◆ **英語の三枚も門番の走査に入れた** —— そのとき被覆の型が **日本語の助数詞しか見ていない**ことが露見した
  （`407×` も `6,103,390 → 15,011` も素通り）。英語の量と `→` を足した。⇒ 台帳は **53 件 / 飛ばし 0**。
- ◆ **影の中だけで七門が走ることを撃った** —— 公開側の「Don't trust this README. Run it.」は本当に走る。
- ⚠️ 影の道は**本体と同じに保つ**と決めた。台帳・門番・再現コマンドが全部道を持つので、
  名を変えると影の中で門番が文書を見つけられない（実際に一度そうなった）。表紙だけ根に複製する。

---

◆ **昇格の条件は機械で書ける**: ① 全実装が `cases.json` を通る ② bench に**数字が出ている**
③ 契約を変えていない。▲ **降格もある**(秤を通らなくなったら育成場へ戻す)。
◆ 判定は一行: **契約がデータで書けるものは共通、身元が語るものは各家。**
切り出さない: **所有の thesis**(身元)/ **魂**(advisory か enforce か)/ **front-end の符号化**。
✅ **名は付いた —— `symbolon`。** ⚠️ その手前で **わたしは古い条項を引いていた** —— 運用の取り決めは
既に改訂されていて、命名は撃ってよい側へ移っていた。**改訂された条文が毎回 画面に出ていたのに、記憶で上書きした。**
🔴 **型: 取り決めの条項も、引く前に撃つ。** 数は門番が見るようになったが、**引用は誰も見ていなかった**(三度目)。

---

## 6. 「速い」の軸 —— 決着した表

| 軸 | 効き方 | 実測 | 残り |
|---|---|---|---|
| ① host 言語の交換 | 定数倍 | 8.4x | 終わった |
| ② 床の内部表現 | 定数倍 | 4.2x | ほぼ終わった |
| ③ **機械のコンパイル** | **漸近** | 命令 **407 / 411 / 426 分の 1**(整数・閉包・cons) | 🔴 本命。**値の種類に依らないと確認済み** |
| ④ 出力 backend | 定数倍 | 段F 7〜11x / 段G は段F の ≈13x | 割付は**既製 engine に任せる**のが得。~~pair の壁~~ ⇒ **七段目で崩れた**(§1-4) |
| ⑤ 幅(SIMD)/ wasm2c | 定数倍 | #5 で 3.55x / 2.63x | 在る。床でなく kernel の話 |

**③ が全てを飲む。** ⇒ **設計判断は一つ: *コンパイルできる形で言語を設計する*。**
所有(§4)はその情報源、契約(§0①)はその共通の的、投影(§3)はその面。

---

## 7. 次の一手

◆ 十一〜十二段で **道が通り、扉に名が付いた** —— 契約に版と錠、昇格試験に七門、
  昇格先に **`symbolon/`**(割符)。⚠️ **判定は今 ⏸ 保留**(通過 5 / 落ち 0 / 保留 2)。

1. ~~昇格先の器を作る~~ ✅ **十二段で立てた**。~~試験が保留~~ ⇒ **十五段で七門すべて通った**。
   ⇒ 残るのは **中身を移すこと**だけ。⚠️ 移すと道が変わる(台帳・門番・再現コマンドが全部道を持つ)
     ∴ **公開の影より先に動かす**のが安い(影の道が二度変わらない)。
   ⚠️ `koinon/` には入れられない —— あちらは ownlang が SOT の vendor copy で、
   drift guard が「SOT に無いファイル」を弾く。**機構が正しく守っている** ∴ 別の器が要った。
2. **文字列を値モデルに入れる** ← **推し**。入口(人向け / AI 向け)と出口を増やす前提が、ここで詰まっている。
   ⟨撃つ前の予想⟩ 畳みの効き(407 / 411 / 426)は**落ちない**(値の種類にほぼ依らないことは三本で測ってある)。
   落ちるとしたら**出口の側** —— 可変長を bump heap だけでは扱えない。外れたら記録する。
3. **op 自体を減らす** —— 底は「解釈の軸」での底。吐く op を減らす余地は残る(register 割付・peephole)。
4. **高さ非依存を自分で撃つ** —— 塔(h=2/h=3)を建てて A が高さに依らないことを *こちらの程式で* 確かめる。
5. **値を返す `if` / 言語 → host** —— 合流で残った門番二本。R5 の残り半分。

▲ 推しの理由: 2 が **入口・出口・ノート応用・skills の門番** の全部に効く一点で、
  しかも「持たない選択」を**代金つきで選べる**ようにする手だから。1 は環境待ちで、手は空いている。

---

## 付録 — 実物

| 何 | どこ |
|---|---|
| 床A→D / 段E(特殊化・**非 escape 閉包の inline**)/ 段F(x86-64 JIT)/ 段G・**段G′(値の形を静的に推し pair を線形メモリへ)**・**段G″(`--own`/`--own2` = 一意所有で回収)**/ 段H(`--engine`)/ `--probe` | `lab/floor_ladder.rs` |
| 畳みを V8 で測る二点目 | `lab/floor_ladder.mjs` |
| 反証テストの素材(**撃つ前の予想がここに書いてある**)/ 走り続ける器 `probe_life` | `probe_emit.py` |
| 段H の素材と **kokkos の機械が出した真値の軌跡** | `lab/engine_emit.py` |
| 段G の秤 / 段H の秤(**実 Chromium の本物のクリック**)/ 段G′ の秤(温めてから測る・線形性) | `lab/web_verify.mjs` / `lab/engine_verify.mjs` / `lab/probe_run.mjs` |
| **合流の門番(姉妹実験 exp/03 の決定的な icount + A(P)。写さず指す)** | `symbolon/icount.sh` |
| **昇格試験(七門。落ちえることを実測済み)/ 契約の錠(版と指紋)** | `lab/promote.sh` / `lab/contract.lock` |
| **主張と門番の突き合わせ(門⑦。台帳 / 免除表)** | `lab/claims.py` / `lab/claims.tsv` / `lab/claims.skip` |
| 生成物 | `symbolon/demo-compiled.html` / `symbolon/demo-engine.html` / `symbolon/engine_seam.json` |

▲ 束縛規則(env→スロット / box→スカラ / 閉包→inline / 死んだ確保を出さない)は段E と段G/H で**二度書いている**。
制御流れの形が違うため back-end を分けた —— **ズレは秤が捕まえる**(同じ答えを出すまで進めない)。
◆ 契約・値モデル・`machine.json` / `cases.json`・melon / koinon / kokkos 本体は**すべて不変**。

# 生成物 —— 一つのコマンドで作り直せる物は追跡しない。
# ⚠️ 影の側でも同じ規則が要る（clone して門番を撃つと、この木が汚れるため）。
#    ⇒ この一枚は名乗り台帳に載せて、影へも出す。
*.wasm
__pycache__/
node_modules/
package*.json

# 差分ファザーの足場（pid 付き。走行ごとに自分の分だけ片付けるが、落ちた時に残りうる）
fuzz_*.json
.fuzz_ladder.*
